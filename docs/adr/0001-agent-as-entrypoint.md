# 通用 Agent 作为操作入口，青笺保存长期数据

用户选择继续在 Codex 等通用 Agent 中理解通知并组织行动，青笺负责长期保存、手动查看与业务校验。采用本地 MCP 适配器调用青笺 HTTP 接口，避免在项目内重复建设模型执行系统，也避免 Agent 直接写数据库绕过账号和版本校验。

提醒执行仍由青笺负责；本次不把 MCP 接入误认为后台推送已经实现。独立待办和课表保持兼容，不因接入 Agent 迁移旧数据。
